<div class="card mb-4">
    <div class="card-header">Admin Control Widget</div>
    <div class="card-body">
        <div>
            <%= link_to "Create New Article", new_article_path, class: "btn btn-primary mb-2" %>
        </div>
        <div>
            <%= link_to "Sign Out", destroy_admin_session_path, data: { turbo_method: :delete, turbo_confirm: "Sing Out?" }, class: "btn btn-danger mb-2"%>
        </div>
    </div>
</div>