<!-- Featured blog post-->
<% if @featured %>
    <%= render 'articles/parts/featured' %>
<% end %>
<!-- Nested row for non-featured blog posts-->
<%= render 'articles/parts/blog_post' %>
<!-- Pagination-->
<%= render 'articles/parts/pagination' %>

<!-- 
<h1>Articles</h1>

Our blog has <%= Article.public_count %> posts!
<ul>
    <% @articles.each do |article| %>
        <% unless article.archived? %>
            <li>
                <%= link_to article.title, article %>
            </li>
        <% end %>
    <% end %>
</ul>

<%= link_to "New Article", new_article_path %>
-->